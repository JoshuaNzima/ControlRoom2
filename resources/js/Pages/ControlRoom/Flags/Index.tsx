// TSX shim to re-export the JS implementation so the page resolver can find a .tsx file
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore
import FlagList from './Index.jsx';

export default FlagList;
